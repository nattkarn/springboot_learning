server:
  port: 8080
spring:
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: "my-awesome-app"
  mail:
    host: smtp.gmail.com
    port: 587
    username: nattkarn.dev
    password: kqtvwjkelsmpnwhs
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  redis:
    lettuce:
      client:
        url: redis://localhost:6379/0
  cache:
    type: redis
    redis:
      time-to-live: 300000


  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/backend
    username: postgres
    password: syspass
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 25MB
---
app:
  token:
    secret: Arm@1144nb
    issuer: BackendService
  email:
    form: nattkarn.dev@gmail.com





